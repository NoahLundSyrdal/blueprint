[metadata]
name = random-python-setup-cfg-pkg
version = 0.1.0

[options]
package_dir =
    = src
packages = find:
install_requires =
    flask>=3.0

[options.packages.find]
where = src

[tool:pytest]
testpaths = tests
