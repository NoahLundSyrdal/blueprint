[project]
name = "blueprint-car-company-demo"
version = "0.1.0"
description = "Small Python car company project used to test Blueprint project-to-UML generation."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
