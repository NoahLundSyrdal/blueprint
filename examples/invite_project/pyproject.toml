[project]
name = "blueprint-invite-demo"
version = "0.1.0"
description = "Small Python project used to test Blueprint project-to-UML generation."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
